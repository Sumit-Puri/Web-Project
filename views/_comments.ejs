<li>
    <p>
        <% if(locals.user && locals.user.id==comment.user.id)  { //user signed in or not%>
                <small>
                        <a href="/comments/destroy/<%= comment.id %>">del</a>
                </small>
                <% } %>
        <%= comment.content %>
        <br>
        <small>
                <%= comment.user.name%>
        </small>
</p>
</li>